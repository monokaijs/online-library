export enum Status {
  IDLE = 'idle',
  PENDING = 'pending',
  FULFILLED = 'fulfilled',
  REJECTED = 'rejected'
}

export enum FormAction {
  VIEW = 'view',
  CREATE = 'create',
  UPDATE = 'update',
  REMOVE = 'remove'
}
